@inject NavigationManager Navigation

@code
{
    protected override Task OnAfterRenderAsync(bool firstRender)
    {
        if(!firstRender) return Task.CompletedTask;
        
        Navigation.NavigateTo("/_auth/login");
        return Task.CompletedTask;
    }
}